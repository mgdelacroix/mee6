database: state.db

hosts:
  targaryen:
    uri: targaryen
  kaleidos.net:
    uri: https://kaleidos.net/


checks:
  - name: http status
    cron: "*/10 * * * * ?"
    hosts:
      - kaleidos.net
    module: http-keepalive
    http-method: "head"
    expected-statuses:
      - 200
      - 201

  # - name: disk usage on /dev/md1
  #   hosts:
  #     - targaryen
  #   cron: "*/10 * * * * ?"
  #   notify: sys
  #   module: disk-usage
  #   device: /dev/md1
  #   threshold: 80
  # - name: docker service
  #   hosts:
  #     - targaryen
  #   cron: "*/10 * * * * ?"
  #   notify: sys
  #   module: service
  #   service: docker
  # - name: special script
  #   hosts:
  #     - targaryen
  #   cron: "*/10 * * * * ?"
  #   module: script
  #   file: /tmp/myscript

notify:
  sys:
    description: we rock!
    emails:
      - me@example.com

mail:
  from: mee6@example.com
  mode: console  # local /smtp / console

http:
  port: 3001
